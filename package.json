{
  "name": "qlab-cue-viewer-web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Web viewer that subscribes to relay-server and renders the current QLab cue.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  }
}
